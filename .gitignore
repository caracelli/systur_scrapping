# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Credenciais
.env
credenciais.json

# Saídas geradas
saida/*.xlsx
saida/*.xls

# Fila de processamento (estado de execução — não versionar)
processamento/

# Artefatos de build do PyInstaller (gerados por gerar_exe.py).
# Ignora tudo em dist/ EXCETO o zip distribuivel, que vai versionado
# via Git LFS para ser baixado em outra maquina sem rebuild.
dist/*
!dist/SysTur_Scrapper.zip
_build_temp/
*.spec

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDEs
.vscode/
.idea/
