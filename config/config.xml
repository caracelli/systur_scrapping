<?xml version="1.0" encoding="UTF-8"?>
<config>

  <!-- true = roda sem abrir janela | false = mostra o Edge na tela -->
  <headless>true</headless>

  <!-- 0 = processa tudo | N = processa apenas os primeiros N itens (teste) -->
  <limite>50</limite>

</config>
